{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Ninja Build & Run",
            "type": "shell",
            "command": "ninja -C ./build && ./build/ojogo",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}